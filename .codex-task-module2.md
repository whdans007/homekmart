# Task: 신선상품 고객 화면 (Module 2) — mall-fresh-products

먼저 반드시 읽을 것: `docs/02-design/features/mall-fresh-products.design.md` 전체(특히 §1, §3, §4.2, §4.3, §5).
이 문서가 스펙이다. 이 파일에서 지시하는 내용과 Design 문서가 다르면 Design 문서를 우선한다.

## 배경
HOME K MART 몰(쇼핑몰)에 신선상품(과일/채소/정육/수산)을 고객이 주문할 수 있게 만드는 중이다.
- `weight`(저울) 타입: 100g 단위로 수량 선택, 실제 무게는 점포 직원이 나중에(준비중 단계) 입력해서 확정금액이 달라짐 — 그래서 "예상금액"과 "확정금액" 개념이 있다.
- `piece`(낱개/정가) 타입: 그냥 개수 × 고정가. 실측 개념이 아예 없다. 정가상품과 UX가 거의 동일하다고 보면 된다.

다른 세션(Claude Code)이 이미 DB 마이그레이션 + `mall/lib/fresh_cart.php`, `mall/lib/fresh_order.php`, `mall/lib/catalog.php`(신규 함수 추가), `mall/lib/order.php`(확장)를 이 워크트리에 완료해뒀다(커밋은 아직 안 됨, 파일만 복사됨). **이 lib 파일들은 이미 완성된 계약(contract)이니 절대 수정하지 말고 그대로 호출**해서 화면만 만들면 된다.

## 이미 구현된 함수 (그대로 호출만 할 것 — 시그니처 참고)

`mall/lib/fresh_cart.php`:
- `mall_fresh_product_get($mall_fresh_product_id)` — 신선상품 마스터 1건 조회(name_ko/name_en/sale_type/price_per_100g/status/is_sold_out)
- `mall_fresh_cart_add($member_id, $guest_token, $mall_fresh_product_id, $weight_g, $quantity)` — 담기(최종값 지정 방식, sale_type이 weight면 weight_g만, piece면 quantity만 넘김. 나머지는 null)
- `mall_fresh_cart_remove($member_id, $guest_token, $cart_item_id)`
- `mall_fresh_cart_count($member_id, $guest_token)` — 헤더 뱃지용, 정가상품 `mall_cart_count()`와 더해서 쓰면 됨
- `mall_fresh_cart_get_summary($member_id, $guest_token)` — `{items: [...], subtotal, has_sold_out}` 반환. 각 item에 `cart_item_id, mall_fresh_product_id, weight_g, quantity, name_ko, name_en, sale_type, price_per_100g, unit_price, estimated_price, sold_out, image_url` 포함

`mall/lib/fresh_order.php`:
- `mall_fresh_order_items_get_by_order($order_id)` — 주문상세 화면에서 신선 라인 조회용. 각 행에 `product_name_snapshot, sale_type_snapshot, unit_price_snapshot, weight_g, actual_weight_g, quantity, estimated_price, confirmed_price, is_sold_out` 포함. `actual_weight_g`가 null이면 아직 미실측(예상금액만 존재), 값이 있으면 확정 완료.
- (체크아웃 시 신선 라인 생성/장바구니 비우기는 `mall_create_order()` 내부에서 이미 자동으로 처리됨 — 화면에서 따로 호출할 필요 없음)

`mall/lib/catalog.php`:
- `mall_get_eligible_fresh_products($category_id = null, $search = '', $limit = 60)` — 카테고리(하위 소분류 포함)에 맞는 신선상품 목록. 각 행: `id, name_ko, name_en, sale_type, price_per_100g, image_url, display_order`. **주의**: 이 함수는 `mall_get_eligible_products()`(정가상품)와 절대 같은 배열에 섞지 말 것 — id 공간이 다르다(신선상품 id ≠ products.id). 화면에서 두 배열을 각각 받아 카드에 `item_type`으로 구분해서 렌더링해야 한다.

## 이번 작업 범위 (아래 파일만 만들거나 수정할 것)

1. **`mall/fresh_product.php`** (신규) — 신선상품 상세페이지. `mall/product.php`(정가상품 상세페이지, 기존 파일)를 먼저 읽고 최대한 같은 스타일/레이아웃/헤더 include 패턴을 따를 것.
   - `?id=` 쿼리로 `mall_fresh_products.id` 받음
   - weight 타입: 100g 단위 +/- 스테퍼, JS로 실시간 예상금액(`price_per_100g * g/100`) 표시, "100g당 가격" 안내
   - piece 타입: 정수 수량 스테퍼(정가상품 상세페이지와 동일한 UX), 무게 관련 UI 전혀 없음
   - 담기 버튼 → 아래 신규 AJAX 호출
   - `is_sold_out=1`이면 담기 버튼 비활성화 + 품절 표시
   - 이미지: `image_url` 단일 이미지(없으면 플레이스홀더)

2. **`mall/ajax/add_fresh_to_cart.php`** (신규), **`mall/ajax/remove_fresh_cart_item.php`** (신규) — 파일명은 기존 `mall/ajax/add_to_cart.php`, `mall/ajax/remove_cart_item.php`와 짝이 맞도록 지었다. 그 두 기존 파일을 먼저 읽고 완전히 같은 패턴(CSRF, 세션/게스트 토큰 처리, JSON 응답 `{success, data, error}`)을 따를 것. `mall_fresh_cart_add()`/`mall_fresh_cart_remove()` 호출.

3. **`mall/cart.php`** (수정, 기존 화면 확장) — 정가상품 라인 아래(또는 구분해서)에 신선상품 라인 표시. `mall_fresh_cart_get_summary()` 호출. weight 타입 라인에는 "예상금액이며 실제 무게에 따라 달라질 수 있습니다" 안내 문구. 합계에 신선상품 subtotal도 포함해서 보여줄 것(최종 확정 금액은 서버가 체크아웃/주문에서 계산하지만, 이 화면은 예상 합계를 보여주면 됨).

4. **`mall/order_checkout.php`** (수정) — 체크아웃 화면에 신선상품 라인도 함께 표시(장바구니와 유사하게). 실제 주문 생성 로직은 이미 `mall_create_order()`가 내부적으로 처리하므로 **호출부는 건드릴 필요 없고 화면 표시만 추가**하면 됨.

5. **`mall/order_detail.php`** (수정) — 주문상세에 신선 라인 표시. `mall_fresh_order_items_get_by_order($order_id)` 호출. `actual_weight_g`가 null이면 "예상 {weight_g}g · 예상금액 {estimated_price}" 배지, 값이 있으면 "확정 {actual_weight_g}g · 확정금액 {confirmed_price}" 배지로 구분 표시. piece 타입은 실측 개념이 없으니 그냥 확정금액만 표시.

6. **`mall/category.php`** (수정) — 44~45번째 줄 근처(`mall_get_eligible_products()`/`mall_build_product_cards()` 호출부)에서 `mall_get_eligible_fresh_products($effective_category_id, $search)`도 함께 호출해서, 신선상품 카드를 정가상품 카드와 같은 그리드에 함께 렌더링하되 "신선" 뱃지를 붙이고 클릭 시 `fresh_product.php?id=`로 이동하게 만들 것. 정가상품 카드 렌더링에 쓰는 partial(예: `partials/product_card.php` 등, 실제 파일명은 확인)이 있다면 그걸 신선상품에도 재사용할 수 있는지 먼저 확인하고, 구조가 너무 다르면 신선상품 전용 카드 마크업을 category.php 안에 인라인으로 추가해도 된다(과도한 추상화 금지).

7. **`lang/ko.json`, `lang/en.json`** — 위 화면에서 필요한 새 문구 키를 쌍으로 추가(신선/과일/채소/정육/수산 관련 문구는 이미 `mall_fresh_products.*` 네임스페이스에 상당수 있으니 먼저 grep해서 재사용 가능한 키부터 찾을 것 — 없는 것만 새로 추가).

## 하지 말아야 할 것
- `mall/lib/fresh_cart.php`, `mall/lib/fresh_order.php`, `mall/lib/catalog.php`, `mall/lib/order.php` 수정 금지(이미 완성됨)
- `mall/admin/*` 파일 건드리지 말 것(다른 담당)
- DB 마이그레이션 실행하지 말 것(이미 준비됨, 실행은 사용자가 직접 함)
- `git commit`은 이 워크트리(`codex/mall-fresh-order-flow` 브랜치)에서만, 완료 후 한 번 수행. `main`으로 병합/푸시하지 말 것.

## 완료 기준
- 작성/수정한 모든 PHP 파일이 `php -l` 통과
- 위 6개 파일(+lang 2개) 외에는 건드리지 않았는지 `git status`로 확인
- 작업 끝나면 아래처럼 커밋 1개로 정리:
  `git add -A && git commit -m "<한글 커밋 메시지 요약>"`
