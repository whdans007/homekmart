# 필리핀 배달 앱 데이터베이스 설계 문서

## 개요
HOME K MART 필리핀 배달 앱을 위한 데이터베이스 스키마 설계 문서입니다. COD(Cash on Delivery) 결제 중심의 배달 서비스를 지원하는 구조로 설계되었습니다.

## 주요 특징
- **COD 결제 중심**: 필리핀 현지 결제 문화 반영
- **구글 소셜 로그인**: 간편한 사용자 인증
- **필리핀 주소 체계**: 바랑가이, 랜드마크 기반 주소 관리
- **다국어 지원**: 영어/한국어 지원
- **배달 추적**: 실시간 주문 상태 관리

## 새로 추가되는 테이블

### 1. delivery_addresses (배달 주소 관리)
필리핀 주소 체계에 맞춘 배달 주소 관리 테이블

**주요 필드:**
- `barangay`: 바랑가이 (필리핀 최소 행정구역)
- `landmark`: 랜드마크 (필리핀에서 주소 찾기에 중요)
- `latitude/longitude`: GPS 좌표
- `delivery_notes`: 배달 메모

**특징:**
- 사용자당 여러 주소 저장 가능
- 기본 주소 설정 기능
- GPS 좌표를 통한 정확한 위치 정보

### 2. delivery_zones (배달 지역 관리)
배달 서비스 가능 지역 및 배달비 관리

**주요 필드:**
- `delivery_fee`: 지역별 배달비 (PHP)
- `free_delivery_threshold`: 무료 배달 최소 금액
- `estimated_delivery_time`: 예상 배달 시간
- `service_start_time/service_end_time`: 서비스 시간

**특징:**
- 지역별 차등 배달비 적용
- 시간대별 서비스 관리
- 도시/주 단위 지역 구분

### 3. delivery_orders (배달 주문)
COD 결제 중심의 주문 관리 테이블

**주요 필드:**
- `payment_method`: 결제 방법 (COD, GCash, PayMaya 등)
- `cod_amount`: COD 결제 금액
- `change_amount`: 거스름돈
- `order_status`: 상세한 주문 상태 관리

**주문 상태 플로우:**
1. `pending` → 주문 접수 대기
2. `confirmed` → 주문 확인
3. `preparing` → 상품 준비 중
4. `ready_for_delivery` → 배달 준비 완료
5. `out_for_delivery` → 배달 중
6. `delivered` → 배달 완료

### 4. delivery_order_items (주문 상품)
주문별 상품 상세 정보

**특징:**
- 주문 시점의 상품 정보 스냅샷 저장
- JSON 형태의 상품 옵션 지원
- 가격 변동에 영향받지 않는 주문 기록

### 5. delivery_tracking (배달 추적)
실시간 배달 상태 추적

**주요 필드:**
- `status`: 상세한 배달 상태
- `latitude/longitude`: 배달원 현재 위치
- `status_message`: 상태별 메시지

**추적 가능한 상태:**
- 주문 접수, 확인, 준비, 픽업, 배달 중, 완료 등

### 6. shopping_cart (장바구니)
사용자별 장바구니 관리

**특징:**
- 점포별 장바구니 분리
- 사용자-상품-점포 조합의 유니크 제약

### 7. wishlists (위시리스트)
사용자 관심 상품 관리

### 8. delivery_settings (배달 설정)
앱 전체 배달 관련 설정 관리

**기본 설정값:**
- 기본 배달비: ₱50
- 무료 배달 최소 금액: ₱1,000
- 최대 배달 거리: 20km
- 기본 배달 시간: 60분

## 기존 테이블 확장

### users 테이블 확장
구글 로그인 및 배달 앱 기능 지원을 위한 컬럼 추가:

- `google_id`: 구글 계정 고유 ID
- `auth_provider`: 인증 제공자 (email/google)
- `profile_image_url`: 프로필 이미지 URL
- `default_delivery_address_id`: 기본 배달 주소
- `preferred_language`: 선호 언어 (en/ko)
- `is_delivery_available`: 배달 서비스 이용 가능 여부

## 다국어 지원 설계

### 언어 지원
- **영어 (en)**: 기본 언어
- **한국어 (ko)**: 관리자용

### 다국어 필드 처리
- 기존 필드는 영어 우선
- 한국어는 별도 필드 또는 JSON 형태로 저장
- 클라이언트에서 언어별 표시 처리

## 통화 및 결제

### 통화
- **PHP Peso (₱)**: 모든 금액 필드
- `DECIMAL(10, 2)` 형태로 저장 (최대 9,999만 페소)

### 결제 방법
1. **COD (Cash on Delivery)**: 기본 방식
2. **GCash**: 필리핀 모바일 결제 (선택사항)
3. **PayMaya**: 필리핀 디지털 월렛 (선택사항)
4. **Online**: 기타 온라인 결제 (향후 확장)

## 인덱스 최적화

### 주요 인덱스
- 사용자 ID 기반 인덱스
- 주문 상태별 인덱스
- 위치 기반 인덱스 (위도/경도)
- 날짜별 인덱스
- 구글 ID 인덱스

## 보안 고려사항

### 데이터 보호
- 구글 ID는 유니크 제약으로 중복 방지
- 배달 주소는 사용자별로 접근 제한
- 주문 정보는 해당 사용자만 조회 가능

### 외래키 제약
- `ON DELETE CASCADE`: 사용자 삭제 시 관련 데이터 자동 삭제
- `ON DELETE RESTRICT`: 중요 데이터는 삭제 방지
- `ON DELETE SET NULL`: 선택적 관계는 NULL 처리

## 성능 최적화

### 쿼리 최적화
- 자주 조회되는 필드에 인덱스 설정
- 복합 인덱스로 다중 조건 검색 최적화
- 파티셔닝 고려 (주문 데이터가 많아질 경우)

### 데이터 관리
- 주문 상태별 분리 저장 고려
- 오래된 배달 추적 데이터 아카이빙
- 정기적인 인덱스 최적화

## 확장성 고려사항

### 향후 확장 가능 기능
- 배달원 관리 시스템
- 실시간 GPS 추적
- 푸시 알림 시스템
- 리뷰 및 평점 시스템
- 쿠폰 및 할인 시스템

### 스케일링 전략
- 읽기 전용 복제본 활용
- 캐싱 레이어 도입
- API 응답 최적화
- 이미지 CDN 활용

## 마이그레이션 가이드

### 설치 순서
1. `delivery_app_schema.sql` 실행
2. 기존 데이터 마이그레이션 (필요시)
3. 인덱스 성능 모니터링
4. 애플리케이션 코드 업데이트

### 롤백 계획
- 새 테이블은 독립적으로 생성되어 기존 시스템에 영향 없음
- users 테이블 확장은 NULL 허용으로 안전
- 필요시 추가된 컬럼만 제거 가능

## 결론

이 데이터베이스 설계는 필리핀 지역의 배달 문화와 COD 결제 방식을 충분히 고려하여 설계되었습니다. 확장성과 성능을 고려한 구조로 향후 기능 추가에도 유연하게 대응할 수 있습니다.