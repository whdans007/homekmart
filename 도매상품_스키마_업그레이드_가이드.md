# 도매 상품 스키마 업그레이드 가이드

## 개요
도매 상품 관리 시스템이 독립적인 도매 전용 상품 정보를 지원하도록 업그레이드되었습니다.

## 주요 변경사항

### 1. 데이터베이스 스키마 변경
다음 필드들이 `wholesale_products` 테이블에 추가되었습니다:
- `wholesale_name_ko` (VARCHAR 255): 도매용 한국어 상품명
- `wholesale_name_en` (VARCHAR 255): 도매용 영어 상품명  
- `wholesale_skus` (JSON): 도매용 다중 SKU 지원
- `wholesale_description` (TEXT): 도매용 상품 설명
- `updated_at` (TIMESTAMP): 업데이트 시간 추적

### 2. 스키마 업데이트 실행 방법

1. **SQL 스크립트 실행**: `Z:\min\sql\update_wholesale_products_schema.sql` 파일을 실행하세요.

2. **PHP 스크립트 실행** (대안): 웹브라우저에서 `update_wholesale_schema.php`를 실행하세요.

### 3. 새로운 기능들

#### 독립적인 도매 상품명
- 도매 고객에게는 다른 상품명으로 표시 가능
- 한국어/영어 각각 설정 가능
- 기본 상품명과 독립적으로 관리

#### 다중 SKU 지원
- 하나의 도매 상품에 여러 SKU 등록 가능
- JSON 형태로 저장: `["WS001", "WS002", "WS003"]`
- 쉼표로 구분하여 입력 (예: WS001, WS002, WS003)

#### 도매 전용 상품 설명
- 도매 고객 전용 상품 설명 텍스트
- 일반 상품 설명과 별도 관리

### 4. UI 업그레이드

#### 도매 상품 추가/수정 페이지
- 기준 상품 정보와 도매 전용 정보가 분리되어 표시
- 도매 전용 필드들이 파란색 배경으로 구분
- 상품 선택시 도매 필드에 기본값 자동 입력 (수정 가능)

#### 도매 상품 관리 페이지
- 도매 전용 정보 우선 표시
- "도매용" 배지로 커스터마이징된 상품 구분
- 기준 상품 정보도 함께 표시

#### 도매 판매 페이지
- 도매 상품 검색시 도매 전용 정보로 표시
- 다중 SKU 지원으로 더 정확한 상품 식별

### 5. 검색 기능 향상

#### 확장된 검색 범위
- 기존: 기준 상품의 이름, SKU만 검색
- 신규: 도매 상품명, 도매 SKU까지 모두 검색

#### 검색 우선순위
1. 도매 SKU 일치
2. 도매 상품명 일치  
3. 기준 상품 정보 일치

### 6. 데이터 마이그레이션

#### 자동 마이그레이션
기존 도매 상품 데이터는 다음과 같이 자동 변환됩니다:
- `wholesale_name_ko` ← `products.name_ko`
- `wholesale_name_en` ← `products.name_en`  
- `wholesale_skus` ← `["products.sku"]` (JSON 배열)

#### 수동 커스터마이징
마이그레이션 후 각 도매 상품을 수정하여:
- 도매 전용 상품명 설정
- 추가 도매 SKU 등록
- 도매 전용 설명 추가

### 7. 사용 방법

#### 새로운 도매 상품 등록
1. **도매 상품 관리** → **상품 추가**
2. **기준 상품 선택** (기존 products 테이블의 상품)
3. **도매 전용 정보 입력**:
   - 도매용 한국어/영어 상품명 (기본값으로 자동 입력됨)
   - 도매 SKU (여러 개 가능, 쉼표로 구분)
   - 도매 전용 설명
   - 도매가격 및 최소 주문수량

#### 기존 도매 상품 수정
1. **도매 상품 관리** → **수정** 버튼 클릭
2. 도매 전용 정보 섹션에서 필요한 정보 수정
3. 저장

### 8. 주의사항

#### 호환성
- 기존 도매 판매 내역은 그대로 유지됨
- API 호출시 새로운 필드들이 포함될 수 있음

#### 백업 권장
- 스키마 변경 전 데이터베이스 백업 필수
- 테스트 환경에서 먼저 검증 권장

#### 성능 고려사항  
- JSON 필드 검색으로 인한 약간의 성능 영향 가능
- 대용량 데이터베이스의 경우 인덱스 최적화 고려

### 9. 문제해결

#### 스키마 업데이트 실패시
1. 데이터베이스 권한 확인
2. 기존 컬럼 존재 여부 확인
3. 수동 SQL 실행

#### 검색 결과 없음
1. 도매 상품이 등록되어 있는지 확인
2. 검색어가 도매 전용 정보에 포함되어 있는지 확인
3. 점포별 권한 설정 확인

## 업그레이드 완료 확인

### 필수 확인 사항
- [ ] 데이터베이스 스키마 업데이트 완료
- [ ] 기존 도매 상품 데이터 마이그레이션 확인  
- [ ] 새로운 도매 상품 등록 테스트
- [ ] 도매 상품 검색 기능 테스트
- [ ] 도매 판매 기능 테스트

### 추가 권장 사항
- [ ] 기존 도매 상품들의 도매 전용 정보 커스터마이징
- [ ] 다중 SKU가 필요한 상품들 추가 SKU 등록
- [ ] 도매 고객용 상품 설명 업데이트

업그레이드가 완료되면 더욱 유연하고 전문적인 도매 상품 관리가 가능해집니다.